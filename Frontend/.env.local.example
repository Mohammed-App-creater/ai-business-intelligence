# FastAPI backend (the AI BI Assistant API from architecture v1.2)
LEO_BACKEND_URL=http://localhost:8000
LEO_BACKEND_TOKEN=dev-token

# Analytics Warehouse (PostgreSQL) — where chat_feedback table lives
WAREHOUSE_HOST=localhost
WAREHOUSE_PORT=5432
WAREHOUSE_DB=leo_warehouse
WAREHOUSE_USER=postgres
WAREHOUSE_PASSWORD=postgres
